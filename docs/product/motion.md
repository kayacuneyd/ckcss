# CKCSS motion contract

Motion is decorative and progressive. CSS transitions and keyframes own visual
motion; JavaScript may trigger state changes but must not replace CSS animation.
Every repeating or attention-seeking animation needs a reduced-motion rule.
Motion tokens are `--ck-duration-*` and `--ck-ease-*`.
