---
title: FAQ
paths:
  - site/patterns/marketing/faq.html
order: 39
family: faq
wave: 8
---

A full page of frequently-asked questions. A fuller, standalone version of the
2-question demo already at `site/marketing-patterns.html#faq` (left untouched),
same relationship `cta.html`/`trust-signal.html` (wave 2) have to their
marketing-patterns.html counterparts.

## States
Each `<details>` is independently open/closed — native, no JS.

## Semantic HTML & accessibility
Native `<details>`/`<summary>` per question — full keyboard support for free.

## Tokens
`ck-disclosure`.

## Responsive behavior
Single-column stack works at every width with no breakpoint needed.
